module mergeconflictlearn {
	
	//PublicVoidmain
	//now new comments are added
	//new lines is added aginst dev line in local
	//learning conflict resolution v1
	//learning conflict v2 git edit
	//only doing commit not push v1
	//only doing commit not push v2
	//only doing commit not push v3
	//only doing commit not push v4
	//only doing commit not push v5
	//only doing commit not push v6
	//only doing commit not push v4dsd
	//only doing commit not push v8

}
